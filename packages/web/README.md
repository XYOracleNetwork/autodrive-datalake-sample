# Web package

Reserved for the React browser application described in the root implementation
plan. No UI, wallet flow, or package manifest exists yet. Add the React/DOM
TypeScript and ESLint configuration when implementing this workspace. Provider
credentials must never enter this package or its browser bundle.
