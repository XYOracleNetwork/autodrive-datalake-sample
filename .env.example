# Optional when --autoDriveKey is passed. Keep credentials out of tracked files.
AUTODRIVE_API_KEY=
# Optional namespace bucket; default: autodrive-sample.
# AUTODRIVE_BUCKET=autodrive-sample

# XL1 signing uses your separately configured AriesTools CLI wallet.
# No seed phrase, private key, RPC URL, or chain ID belongs here.
